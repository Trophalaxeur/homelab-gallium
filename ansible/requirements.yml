---
# Collections required by this repo.
# Install on the control node before running playbooks:
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.0"
